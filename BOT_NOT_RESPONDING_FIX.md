# Бот не отвечает - Пошаговая инструкция по исправлению

## ✅ Что уже проверено и работает:

1. ✅ Токен бота валидный
2. ✅ Webhook установлен правильно: `https://find-origin.vercel.app/api/telegram`
3. ✅ Endpoint доступен и отвечает

## 🔴 Основная проблема:

**Скорее всего, переменная `TELEGRAM_BOT_TOKEN` не установлена на Vercel или проект не переразвернут.**

## 📋 Пошаговая инструкция:

### Шаг 1: Проверьте переменные окружения на Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект `FindOrigin`
3. Перейдите в **Settings** → **Environment Variables**
4. Найдите переменную `TELEGRAM_BOT_TOKEN`

**Если переменной НЕТ:**
- Нажмите **Add New**
- **Name:** `TELEGRAM_BOT_TOKEN`
- **Value:** `6825751325:AAGrU8yECxlw6YlH8VBXDyRwYmqdHhf3Z3k`
- Выберите **Production**, **Preview**, **Development** (или все три)
- Нажмите **Save**

**Если переменная ЕСТЬ:**
- Убедитесь, что значение точно: `6825751325:AAGrU8yECxlw6YlH8VBXDyRwYmqdHhf3Z3k`
- Убедитесь, что она включена для **Production**

### Шаг 2: Переразверните проект

**ВАЖНО:** После добавления/изменения переменных окружения проект нужно переразвернуть!

1. В Vercel Dashboard перейдите в **Deployments**
2. Найдите последний deployment
3. Нажмите на три точки (⋮) рядом с deployment
4. Выберите **Redeploy**
5. Подтвердите переразвертывание

**Или:**
- Сделайте любой коммит в Git и запушьте изменения
- Vercel автоматически переразвернет проект

### Шаг 3: Проверьте логи

1. В Vercel Dashboard перейдите в **Deployments**
2. Откройте последний deployment
3. Перейдите на вкладку **Logs**
4. Отправьте `/start` боту в Telegram
5. Ищите записи `[WEBHOOK]` в логах

**Что искать в логах:**

✅ **Хорошие признаки:**
```
[WEBHOOK] POST запрос получен
[WEBHOOK] Получен webhook от Telegram
[WEBHOOK] Обработка команды /start
[TELEGRAM] sendMessage вызван
[TELEGRAM] Сообщение успешно отправлено
```

❌ **Плохие признаки:**
```
TELEGRAM_BOT_TOKEN не установлен
Error: TELEGRAM_BOT_TOKEN не установлен
```

### Шаг 4: Если в логах нет [WEBHOOK] записей

Это означает, что Telegram не отправляет запросы на ваш endpoint.

**Решение:**
1. Переустановите webhook:
   ```powershell
   .\setup-webhook.ps1
   ```

2. Или вручную через браузер:
   ```
   https://api.telegram.org/bot6825751325:AAGrU8yECxlw6YlH8VBXDyRwYmqdHhf3Z3k/setWebhook?url=https://find-origin.vercel.app/api/telegram
   ```

3. Проверьте статус webhook:
   ```
   https://api.telegram.org/bot6825751325:AAGrU8yECxlw6YlH8VBXDyRwYmqdHhf3Z3k/getWebhookInfo
   ```

### Шаг 5: Если в логах есть ошибки

**Ошибка: "TELEGRAM_BOT_TOKEN не установлен"**
- Убедитесь, что переменная добавлена на Vercel (Шаг 1)
- Убедитесь, что проект переразвернут (Шаг 2)

**Другие ошибки:**
- Скопируйте текст ошибки
- Проверьте, что все необходимые переменные окружения установлены

## 🧪 Быстрая проверка

Запустите диагностику:
```powershell
.\fix-bot-now.ps1
```

## 📞 Дополнительная помощь

Если проблема не решена:

1. Проверьте логи Vercel после отправки `/start` боту
2. Убедитесь, что проект успешно развернут (зеленый статус)
3. Проверьте, что endpoint доступен:
   ```
   https://find-origin.vercel.app/api/telegram
   ```
   Должен вернуть: `{"status":"ok"}`

## ✅ Чеклист

- [ ] Переменная `TELEGRAM_BOT_TOKEN` установлена на Vercel
- [ ] Значение переменной: `6825751325:AAGrU8yECxlw6YlH8VBXDyRwYmqdHhf3Z3k`
- [ ] Переменная включена для Production
- [ ] Проект переразвернут после добавления переменной
- [ ] Webhook установлен: `https://find-origin.vercel.app/api/telegram`
- [ ] В логах Vercel есть записи `[WEBHOOK]` при отправке сообщения боту
